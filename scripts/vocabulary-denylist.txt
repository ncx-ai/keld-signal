# Retired names from the 2026-09-23 rename, as amended by Revision 3 on 2026-09-25
# (docs/superpowers/specs/2026-09-23-multi-group-attribution-discovery.html, AC-11 + R3-AC-1).
# Signal says project (its own bucket), group (a set of projects) and dimension
# (repo/branch/model...). An Atlas workstream is Atlas's value — held, never shown.
# One extended regex per line; matched per line in Go, Python, JS, TS, HTML, CSS and shell sources.
# A line carrying the marker `vocab:keep` is exempt: it is reading or naming an OLD stored name on purpose.
#
# Names that are NOT here on purpose — the keep list: the Atlas wire keys `projects`,
# `projects_status`, `project_matches` and the facet key `workstreams`; the sidecar route
# `POST /projects` and its `projects` body key; the dimension named `project` (workspace
# basename); 3.0.6's stored names (`projects.json` with its `workstreams`/`workstream` keys,
# `workstreams_off`, marked vocab:keep where used); anything about Claude Code's
# ~/.claude/projects directories or a .keld.toml project.

# --- Go: Revision 1's name for Signal's project (retired by Revision 3) ---
internal/agent/workstreams\b
\bworkstreams\.(Workstream|Store|Document|Attribute|Candidates|Load|Save|Rule|Rollup)\b
\bRemoteWorkstreams?\b
\bFromRemoteWorkstreams\b
\bWorkstreamAttribution\b
\bWorkstreamMatch(es)?\b
\bLoadWorkstreamsFile\b
\bErrWorkstreamNotFound\b
\bMapWorkstreamTo\b
\bEnvWorkstreamsFile\b
\bworkstreamMatchesFor\b
\bWorkstreamTotal\b|\bWorkstreamID\b|\bOutcomeWorkstream\b
\bWorkstreamsAttributed\b|\bWorkstreamsPending\b|\bWorkstreamsSkipped[A-Za-z]*\b|\bWorkstreamsDegradedWeights\b
# --- Go: group sense (was workstream) ---
\bWorkstreamKey\b
\bWorkstreamOffFunc\b
\bSetWorkstreamOff\b
\bErrWorkstreamOff\b
\bWorkstreamsOff\b
\bWorkstreamOrigin(Atlas|Local)\b
# --- Go: dimension sense (was workstreams) ---
\bWorkstreamsEligible\b
\bWorkstreamsExtractor\b
\bWorkstreamAnalyzer\b
\bWorkstreamSpanMinutes\b
\bKnownWorkstreamStatus\b
\bWorkstreamStatuses\b
\bWorkstreamAttributed\b
# --- Python ---
\bapp\.analysis\.workstreams\b
\bset_workstreams\b
\bworkstream_vectors\b
\bcurrent_workstreams\b
\bworkstream_doc\b
\bSTATUS_SKIPPED_NO_WORKSTREAMS\b
# --- local routes, stored names, env ---
/v1/workstreams\b
workstreams_off
KELD_WORKSTREAMS_FILE
workstreams\.json
\.pre-rename\b
# --- page copy and page identifiers ---
"New workstream"
"Your workstreams"
"No workstreams yet\."
"Workstreams on"
"Workstream", "Tokens"
"no workstream"
\bworkstreamCellInfo\b|\bworkstreamTitle\b|\brenderWorkstreams\b|\bsetWorkstreamOff\b|\bworkstreamDisplayName\b|\bworkstreamsOf\b
workstream-row|#/workstreams\b|workstream_id
# the spec files Revision 1 moved (a Playwright project regex once kept the old name)
map-workstream\.spec|\bworkstreams\.spec\.ts|new-workstream\.test
