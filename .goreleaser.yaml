version: 2

project_name: keld

before:
  hooks:
    - go mod tidy

builds:
  - id: keld
    main: ./cmd/keld
    binary: keld
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/ncx-ai/keld-signal/internal/version.CLI={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

  - id: keld-agent
    main: ./cmd/keld-agent
    binary: keld-agent
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/ncx-ai/keld-signal/internal/version.CLI={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

  # ⚠️ WINDOWS ONLY, and that is not an oversight — this is the Inno wizard
  # page's helper. It hosts the WebView2 approval panel and runs `keld`
  # asynchronously, neither of which Pascal Script can do for itself. macOS has
  # a native Installer.app pane and Linux's install.sh has no wizard at all, so
  # building it there would ship a binary nothing could ever run.
  - id: keld-wizard-host
    main: ./cmd/keld-wizard-host
    binary: keld-wizard-host
    env:
      - CGO_ENABLED=0
    ldflags:
      # ⚠️ -H windowsgui, or the helper flashes a console window every time the
      # wizard page runs a step — which is several times per install, on the one
      # screen whose whole purpose is that no console ever appears.
      - -s -w -H windowsgui -X github.com/ncx-ai/keld-signal/internal/version.CLI={{.Version}}
    goos:
      - windows
    goarch:
      - amd64

archives:
  - id: keld
    ids:
      - keld
      - keld-agent
      - keld-wizard-host
    name_template: "keld_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: "checksums.txt"

# Auto-generated release-body changelog: group by conventional-commit type and
# drop docs/test/chore/ci/style/merge noise. (The curated human changelog lives
# in CHANGELOG.md.)
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs"
      - "^test"
      - "^chore"
      - "^style"
      - "^ci"
      - "^build"
      - "^Merge "
      - "go mod tidy"
  groups:
    - title: Features
      regexp: "^.*feat(\\(.+\\))?:"
      order: 0
    - title: Fixes
      regexp: "^.*fix(\\(.+\\))?:"
      order: 1
    - title: Other changes
      order: 999

release:
  # Tags with a prerelease segment (e.g. v0.3.0-rc.1) are flagged as GitHub
  # pre-releases, so they're not served as "latest" (the install.sh one-liner
  # resolves `releases/latest`, which excludes pre-releases). Unsigned installers
  # are produced whenever the signing secrets are absent — the prerelease default.
  prerelease: auto
  github:
    owner: ncx-ai
    name: keld-signal
