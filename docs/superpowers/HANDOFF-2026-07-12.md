# Session Handoff — 2026-07-12 (keld-cli)

Big multi-feature session. This captures state so a fresh context can resume.

## Where we are RIGHT NOW
- **Branch:** `feat/signal-client-telemetry` (sub-project A — **specced + planned, NOT executed**).
- `main` has everything below merged. The branch adds only 3 doc commits (spec + plan for A).
- **Uncommitted/untracked (safe to ignore or clean):** `build/` + `dist/` (~GB, from local obfuscated-freeze runs — `rm -rf build dist` to clear); `installers-*` dirs + `docs/superpowers/plans/2026-07-02-job-categories-*.md` predate this session. The sidecar venv (`~/.keld/sidecar-venv`) has build tools (pyarmor/pyinstaller/python-minifier) installed from local freeze runs — harmless; `make sidecar` recreates a clean one.

## Shipped to `main` this session (in order)
1. **`fix/sidecar-footprint-and-metrics`** (`be33cd8`): daemon injects `MALLOC_ARENA_MAX=2` + OMP/MKL/... thread caps into the sidecar spawn env (RSS 6.4→2.8 GB, CPU→2 cores); sidecar `/metrics` gained live `rss_mb`; `keld-agent metrics` + `keld-agent enrich` commands; `agentcfg.SidecarPort`; **`keld signal status` now shows local-service health**, + `keld signal enrich`/`metrics` via shared `internal/localagent`.
2. **`feat/sidecar-idle-trim`** (`eb81b49`): idle `malloc_trim` in memwatch. **SUPERSEDED** by #3 (memwatch + idle-trim were removed) — don't resurrect.
3. **`feat/sidecar-inference-worker`** (`1294096`) — the big one: **inference moved to a recyclable child process**. FastAPI parent holds no model/torch (~50 MB flat RSS); `internal/agent... sidecar/app/worker.py` + `worker_manager.py` own a `multiprocessing`-spawn worker recycled on RSS-ceiling/idle/pressure/timeout/crash. Removed in-process model + memwatch + idle-trim. `/metrics` reworked to a `worker` block. Fixed `/health` deadlock (ok = serve-on-demand) + idle-disable bug, both caught by live verification.
4. **`fix/sidecar-worker-liveness-poll`** (`54861c5`): `call()` polls in slices so a mid-job worker death is caught in ~1s; HELD-shed counted; cleanups.
5. **`feat/build-obfuscation`** (`c9f5cfd`): **`KELD_OBFUSCATE=1`** (CI-set, default off) → python-minifier locals-rename → free PyArmor → PyInstaller; Go `-s -w` (already via GoReleaser). Found+fixed a **latent frozen-worker-spawn bug (`multiprocessing.freeze_support()` in serve.py)** — affected the plain shipped sidecar too. Added `make freeze-check` (plain) + `make obfuscate-check` (obfuscated) local runners + a CI `/classify` spawn smoke. Validated live on Linux.
6. **`feat/dependency-retry`** (`187b71d`): **`internal/retry`** (exp-backoff + jitter, canonical `IsTransient` classifier, env `KELD_RETRY_*`, Retry-After); wired into the **HF model download** (transient retry, 404 fast-fail). `settings`/`publish`/`api` adopt-as-touched.

## Key durable conventions established (respect these)
- **Memory model:** inference runs in a killable/recyclable child process; the service RSS stays flat. Any change touching worker/spawn/freeze MUST keep `make freeze-check`/`obfuscate-check` green (frozen spawn only breaks in the frozen binary — unit tests can't catch it; `freeze_support()` is load-bearing).
- **Retries:** use `internal/retry` for dependency pulls (don't hand-roll backoff); unknown errors = permanent by design.
- **Obfuscation:** locals-only rename (globals/Pydantic-fields/spawn-targets preserved); protects code logic NOT model identity (GLiNER2 discoverable regardless — explicit non-goal); license-ready for paid PyArmor RFT.
- **`/v1/signal/*` route convention:** all client-signal ↔ Atlas protocol routes namespace under `/v1/signal/*` (new work). Existing `/v1/enrichments` + `/v1/enrichment-settings` predate it; migrating them is a coordinated cross-repo follow-up.
- All non-trivial work: brainstorm → spec (`docs/superpowers/specs/`) → plan (`docs/superpowers/plans/`) → subagent-driven execution → whole-branch review → merge to `main` locally (no remote access here; `git pull` fails — that's expected).

## THE ACTIVE TASK: sub-project A (this branch)
"Signal Client monitoring" is a 3-part cross-repo feature for internal admins to see client-side errors/anomalies + reconstruct narratives, on a new dashboard:
- **A (keld-cli, HERE):** client capture + transport. **Spec:** `docs/superpowers/specs/2026-07-12-signal-client-telemetry-capture-design.md`. **Plan (9 TDD tasks):** `docs/superpowers/plans/2026-07-12-signal-client-telemetry-capture.md`. **NOT YET EXECUTED.**
- **B (keld-atlas repo `~/keld/keld-atlas/services/api`):** the `POST /v1/signal/client-events` ingest route + storage + query API. NOT STARTED (separate repo/context).
- **C (keld-atlas `~/keld/keld-atlas/services/admin-web`, Next.js 16/React 19):** the Signal Client monitoring dashboard. NOT STARTED.

A's design decisions (all locked): structured typed events (stable code+severity) emitted alongside `log.Printf`; full correlation (org/actor/stable install-id/run-id/session/prompt/version/os/arch); control-plane governed (`client_telemetry` block, default ON, independent of enrichment); **privacy redaction gate** (no raw prompt text — the masking analogue); **resource watcher** via `gopsutil/v4` sampling the daemon+sidecar+worker tree → sustained-high RAM/CPU anomaly events (escalating, hysteresis) + 5-min gauge snapshots; batch/flush/retry(`internal/retry`)/offline-spool transport to `POST /v1/signal/client-events`; versioned envelope (`clientevents.SchemaVersion`).

## Open follow-ups (tracked)
- **Execute A** (the 9-task plan) → then B, then C (separate keld-atlas repo).
- `/v1/signal/*` migration of existing enrichments/settings routes (cross-repo, breaking — coordinate).
- Obfuscation paid-PyArmor upgrade (unlocks RFT/BCC globals rename) — license-ready.
- (memory) `followup-hf-download-retry.md` — DONE this session (HF retry shipped); the memory can be updated/closed.

## Verify commands
`go build ./...` · `go test ./...` (0 fail expected) · sidecar: `cd sidecar && for f in app/test_*.py; do PYTHONPATH=. ~/.keld/sidecar-venv/bin/python "$f"|tail -1; done` · frozen spawn: `make freeze-check` / `make obfuscate-check` (heavy, Linux).
